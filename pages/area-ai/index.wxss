page {
  box-sizing: border-box;
  padding-bottom: calc(env(safe-area-inset-bottom) + 96rpx);
  position: relative;
  background-color: #f7fafe;
}

.login-overlay {
  width: 100%;
  height: 100%;
  --td-overlay-bg-color: white;
}

.icon-wrapper {
  width: 200rpx;
  height: 200rpx;
  display: flex;
  justify-content: center;
  align-items: center;
  background: #ffffff;
  box-shadow: 0 0 30rpx 0 #ebedf0;
  border-radius: 50rpx;
}

.center-text {
  margin-top: 40rpx;
  font-weight: 700;
  font-size: 36rpx;
  color: #1c2023;
  text-align: center;
}

.description-text {
  margin-top: 40rpx;
  font-size: 26rpx;
  color: #1c2023;
  letter-spacing: 0;
  text-align: center;
  margin-left: 24rpx;
  margin-right: 24rpx;
}

.privacy {
  --td-radio-font-size: 28rpx;
  --td-radio-icon-checked-color: var(--theme-color);
  --td-radio-content-line-height: 28rpx;
  --td-radio-label-line-height: 28rpx;
}

.small-font {
  font-weight: 400;
  font-size: 28rpx;
  color: #1c2023;
  --td-radio-font-size: 24rpx;
}

.button::after {
  border: none;
}

.description {
  font-weight: 400;
  font-size: 24rpx;
  color: #8f969c;
}

/* 容器基础样式 */
.container {
  display: flex;
  flex-direction: column;
  box-sizing: content-box;
  width: 100%;
}

/* 滚动视图，包含所有说明内容 */
.intro-scroll-view {
  flex: 1; /* 占据除底部按钮外所有空间 */
  width: 100%;
  padding: 24rpx 0; /* 内部内容左右不贴边，上下留白 */
  box-sizing: border-box;
}

/* 说明内容包裹层，用于设置左右内边距 */
.intro-content {
  padding: 0 40rpx; /* 左右内边距 */
  display: flex;
  flex-direction: column;
  background-color: #fff; /* 内容背景为白色 */
  border-radius: 20rpx;
  margin: 0 24rpx; /* 容器左右外边距，形成居中效果 */
  padding-bottom: 40rpx; /* 底部填充，避免内容紧贴边框 */
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1); /* 阴影效果 */
}

/* 标题样式 */
.main-title {
  font-size: 40rpx;
  font-weight: bold;
  color: #d94e50; /* 数码彩主题色 */
  margin-top: 40rpx;
  margin-bottom: 10rpx;
  text-align: center;
  display: block;
}

.sub-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
  margin-bottom: 30rpx;
  text-align: center;
  display: block;
}

.highlight-text {
  font-size: 30rpx;
  color: #007bff; /* 突出显示文字 */
  font-weight: bold;
  text-align: center;
  margin-bottom: 30rpx;
  display: block;
}

/* 段落和列表样式 */
.paragraph {
  font-size: 28rpx;
  color: #333;
  line-height: 1.8;
  margin-bottom: 20rpx;
  text-align: justify;
}

.section-heading {
  font-size: 32rpx;
  font-weight: bold;
  color: #d94e50;
  margin-top: 40rpx;
  margin-bottom: 15rpx;
  text-align: left; /* 左对齐小标题 */
  display: block;
}

.list-item {
  font-size: 28rpx;
  color: #333;
  line-height: 1.6;
  margin-top: 15rpx;
  font-weight: bold;
}

.list-sub-item {
  font-size: 26rpx;
  color: #666;
  line-height: 1.5;
  margin-left: 30rpx; /* 缩进 */
  margin-bottom: 5rpx;
  text-align: justify;
}

.list-sub-item-tip {
  font-size: 24rpx;
  color: #a0a0a0; /* 提示文字颜色 */
  line-height: 1.4;
  margin-left: 30rpx;
  margin-bottom: 10rpx;
  font-style: italic;
}

.footer-signature {
  font-size: 26rpx;
  color: #999;
  text-align: center;
  margin-top: 40rpx;
  font-style: italic;
  display: block;
}

/* 底部按钮条 */
.bottom-bar {
  width: 100%;
  padding: 30rpx 40rpx;
  background-color: #fff;
  box-shadow: 0 -2rpx 10rpx rgba(0, 0, 0, 0.08);
  box-sizing: border-box;
}

.start-game-button {
  width: 100%;
  height: 90rpx;
  border-radius: 50rpx;
  background-color: #007bff; /* 蓝色按钮 */
  color: #fff;
  font-size: 32rpx;
  display: flex;
  justify-content: center;
  align-items: center;
}

.color-tile-container {
  width: 100%;
  height: 100%;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  border-radius: 10px;
}

.color-tile {
  height: 90rpx;
  width: 100%;
  border-radius: 10px;
  margin-bottom: 10rpx;
}

.color-text {
  font-weight: 400;
  font-size: 24rpx;
  color: #1c2023;
}

.share-button {
  background: #edbf58;
  border: hidden;
  color: white;
}

.section-container {
  background-color: white;
  padding: 24rpx;
  border-radius: 20rpx;
  display: flex;
  flex-direction: column;
}

.collapse {
  --td-collapse-header-text-color: #1c2023;
  /* border-right: 1rpx solid var(--theme-color); */
  --td-collapse-title-font-size: 24rpx;
  --td-collapse-horizontal-padding: 8rpx;
  --td-collapse-content-padding: 8rpx;
}

/* 可爱按钮的通用样式 */
.cute-button {
  width: 100%;
  margin-bottom: 24rpx;
  height: 96rpx; /* 稍微小一点，更显可爱 */
  border-radius: 48rpx; /* 圆角更大，更可爱 */
  font-size: 28rpx; /* 字体适中 */
  font-weight: bold;
  color: white;
  display: flex;
  justify-content: center;
  align-items: center;
  box-shadow: 0 3rpx 7rpx rgba(0, 0, 0, 0.2); /* 可爱的浮动感 */
  transition: all 0.2s ease; /* 动画过渡 */
}
.cute-button::after {
  border: none;
}

.cute-button:active {
  transform: translateY(2rpx); /* 按下时下沉 */
  box-shadow: 0 3rpx 8rpx rgba(0, 0, 0, 0.15); /* 阴影变浅 */
  opacity: 0.95; /* 略微透明 */
}

.cute-button .button-icon {
  margin: 0 12rpx; /* 图标与文字间距 */
  font-size: 38rpx; /* 图标大小 */
  line-height: 1;
  vertical-align: middle;
}

.jump-to-ai-btn {
  /* 活泼明亮的橘黄到橙色渐变，适合朋友圈的活力 */
  background: linear-gradient(180deg, #eec461 1%, #e2aa42 100%);
  box-shadow: 5rpx 7rpx 35rpx 0 #0000009c;
}

.retry-button {
  margin-top: 40rpx;
  /* 活力绿色系渐变，给人重新开始的清新感 */
  background: white;
  color: #1c2023;
  box-shadow: 0 6rpx 15rpx rgba(161, 196, 253, 0.35);
}

/* Overlay 容器 */
.share-guide-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.7); /* 半透明黑色背景 */
  z-index: 9999; /* 确保在最上层 */
}

/* 引导内容区域 */
.guide-content {
  position: absolute;
  top: 10rpx; /* 根据实际右上角图标位置调整 */
  right: 86rpx; /* 根据实际右上角图标位置调整 */
  display: flex;
  flex-direction: column;
  align-items: flex-end; /* 让箭头和文字靠右对齐 */
}

/* 箭头图标 */
.arrow-icon {
  color: var(--theme-color);
  animation: moveUpDown 0.7s ease-in-out infinite alternate;
}

/* 提示文字 */
.guide-text {
  color: #fff;
  font-size: 32rpx;
  background-color: rgba(0, 0, 0, 0.6);
  padding: 16rpx 24rpx;
  border-radius: 8rpx;
  max-width: 500rpx; /* 限制文字宽度 */
  text-align: right; /* 文字右对齐 */
}

/* 定义动画的关键帧 */
@keyframes moveUpDown {
  0% {
    transform: translateY(0); /* 初始位置：不偏移 */
  }
  100% {
    transform: translateY(-10px); /* 结束位置：向上移动 50px */
  }
}

.container {
  background-color: #f8f8f8; /* 背景色 */
  box-sizing: border-box; /* 边框盒模型 */
}

.header {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 40rpx;
  padding-bottom: 20rpx;
  border-bottom: 1px solid #eee; /* 分隔线 */
}

.logo {
  width: 120rpx;
  height: 120rpx;
  margin-bottom: 15rpx;
  border-radius: 20rpx; /* 圆角 */
}

.title {
  font-size: 48rpx; /* 大标题 */
  font-weight: bold;
  color: #333;
}

.subtitle {
  font-size: 32rpx;
  color: #666;
  margin-top: 10rpx;
}

.section {
  background-color: #fff;
  border-radius: 16rpx;
  padding: 30rpx;
  margin-bottom: 30rpx;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.05); /* 轻微阴影 */
}

.section-title {
  font-size: 36rpx;
  font-weight: bold;
  color: var(--theme-color); /* 主题色 */
  margin-bottom: 20rpx;
  padding-bottom: 5rpx;
}

.paragraph {
  font-size: 28rpx;
  line-height: 1.6;
  color: #444;
  margin-bottom: 15rpx;
}

.paragraph-bold {
  font-size: 28rpx;
  line-height: 1.6;
  color: #333;
  font-weight: bold;
  margin-top: 10rpx;
  margin-bottom: 5rpx;
}

.feature-list {
  margin-left: 30rpx;
  margin-bottom: 15rpx;
  display: flex;
  flex-direction: column;
  width: 100%;
}

.feature-item {
  display: flex;
  flex: 1;
  align-items: center;
  font-size: 28rpx;
  color: #555;
  width: 100%;
  margin-bottom: 20rpx;
}

.feature-icon {
  font-size: 32rpx;
  margin-right: 15rpx;
  color: #2a7cba; /* 图标颜色 */
}

.feature-text {
  font-weight: bold;
  margin-right: 8rpx;
  color: #333;
  display: flex;
  min-width: 200rpx;
}

.step {
  display: flex;
  margin-bottom: 25rpx;
  align-items: flex-start;
}

.step-number {
  font-size: 36rpx;
  font-weight: bold;
  color: #2a7cba;
  margin-right: 20rpx;
  min-width: 40rpx; /* 确保数字有足够空间 */
  text-align: right;
}

.step-content {
  flex: 1; /* 占据剩余空间 */
}

.step-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
  margin-bottom: 10rpx;
}

.footer {
  text-align: center;
  font-size: 26rpx;
  color: #999;
  margin-top: 40rpx;
  padding-top: 20rpx;
  border-top: 1px dashed #eee;
}

.bold {
  font-weight: bold;
}

.font32 {
  font-size: 32rpx;
  color: #1c2023;
}

.font28 {
  font-size: 28rpx;
  color: #1c2023;
}

.loading {
  --td-loading-color: var(--theme-color);
  --td-loading-text-color: var(--theme-color);
}

.font26 {
  font-size: 26rpx;
  color: #1c2023;
}

.font24 {
  font-size: 24rpx;
  color: #1c2023;
}

.font22 {
  font-size: 22rpx;
  color: #1c2023;
}

.tab {
  margin-top: 30rpx;
  overflow: visible;
  --td-tab-item-active-color: var(--theme-color);
  --td-tab-track-color: var(--theme-color);
}

.header1-container {
  height: 48rpx;
  width: 100%;
  display: flex;
  align-items: center;
}

.header1-text {
  font-size: 34rpx;
  color: #1c2023;
  font-weight: 700;
}

.sticky-container {
}

.sticky-container.active {
  pointer-events: none;
}

.image {
  height: 490rpx;
  width: 100%;
  transition: transform 0.5s ease-in-out, left 0.5s ease-in-out;
}

.image.sticky {
  pointer-events: auto;
  left: 0;
  transform: scale(0.5) translateX(calc(-50% - (100vw - 90vw) / 2));
}
